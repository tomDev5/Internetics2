{
  "name": "internetics2",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start-express": "cd express-server && npm start",
    "start-react": "cd siren && npm start",
    "start-angular": "cd siren-admin && npm start",
    "install-all": "npm install && cd express-server && npm install && cd ../siren && npm install && cd ../siren-admin && npm install",
    "start": "npm run install-all && start npm run start-express && start npm run start-react && start npm run start-angular"
  },
  "dependencies": {
    "concurrently": "^6.0.0"
  }
}
